# Sourced by every runbook: `set -a; . common.env; set +a`
# Secrets are NOT here. Keep them in ~/.bench.env and source that first:
#   TAVILY_API_KEYS (or TAVILY_API_KEY), LANGSMITH_API_KEY
#
# TAVILY_API_KEYS holds one key per account, comma separated. A 50-question
# batch issues thousands of searches and will exhaust a single account's
# credits part-way through; the workflow hands each query to the next key in
# turn so the spend divides evenly, and drops any key the API refuses. Probe
# them before a long run:
#   python tests/check_tavily_keys.py   # in $WORKFLOW_REPO
: "${TAVILY_API_KEYS:=${TAVILY_API_KEY:?set TAVILY_API_KEYS or TAVILY_API_KEY in ~/.bench.env}}"
export TAVILY_API_KEYS
# Probe every key once at the first search, so a dead account is named at
# startup rather than three hours into a batch.
export TAVILY_KEY_HEALTHCHECK=1
# A 429 benches a key for this long; three benchings in a row retire it.
export TAVILY_KEY_COOLDOWN_S=60

# --- paths (edit to match the box) ---
export WORKFLOW_REPO=/home/vibhav/open_deep_research
export VLLM_BASELINE_REPO=/home/vibhav/Build/vllm-baseline
export VLLM_CONTINUUM_REPO=/home/vibhav/Build/vllm-continuum
export VLLM_OURS_REPO=/home/vibhav/Build/KVCOMM-VLLM/vllm

# --- one virtualenv per build ---
# Three vllm checkouts cannot share one site-packages. Unset these and
# `vllm serve` resolves on PATH, so every arm boots whichever build the shell
# activated -- the run completes and compares a stack against itself.
# Defaults only: copy example.env at the repo root to .env, fill it in, and
# source it before this file to override them without editing a tracked file.
export VLLM_BASELINE_VENV=/home/vibhav/venvs/baseline
export VLLM_CONTINUUM_VENV=/home/vibhav/venvs/continuum
export VLLM_OURS_VENV=/home/vibhav/venvs/ours
export WORKFLOW_VENV=/home/vibhav/venvs/odr
export TRACE_DIR=/disk2/vibhav/traces
export REPORT_DIR=/disk2/vibhav/reports
# Blank on purpose: no disk tier. LMCache then runs L1-only, `--l2-adapter` is
# omitted, and restarting the server IS the wipe -- one fewer thing that can be
# half-done between cells. L2 only ever held what spilled past L1, and L1 is
# 100-200 GB of CPU memory against questions of a few tens of thousands of
# prompt tokens, so nothing reached it: the n50 batch peaked at ~38 GB of a
# 200 GB pool. Set a path here to get the tier back, and note that it is then
# the one thing that survives a restart.
export LMCACHE_L2_DIR=
export VLLM_LOG_DIR=/disk2/vibhav/vllm-logs

# --- model / client ---
export MODEL_PROVIDER=openai
export MODEL_NAME=Qwen/Qwen2.5-72B-Instruct-AWQ
export MODEL_TEMPERATURE=0.0
export OPENAI_BASE_URL=http://localhost:8000/v1
export OPENAI_API_KEY=EMPTY
export DISABLE_THINK_TOOL=true

# Node-aware KV eviction for the arm that runs it -- `ours`, and any future
# arm that declares VLLM_NODE_EVICTION_POLICY in arms.toml. 1 is the full
# policy, 0 is the upstream LRU free-block queue, empty leaves the arm's own
# setting alone (today's behaviour, and the default).
#
#   BENCH_NODE_EVICTION=0 ./launch-batch.sh 50
#
# baseline, continuum and record never see it whatever it is set to: they are
# the control, and a variable that could switch the policy on for them would
# be a way to invalidate the comparison without leaving a trace in the
# results. It exists at all because the server's environment is built from
# arms.toml alone -- no env file reaches it -- so the on/off pair would
# otherwise be an edit to a tracked file.
#
# Note 0 is upstream, which means no controller and so no kv_hbm_ttft lines to
# diff against. For an off-arm that still reports, leave this empty and set
# VLLM_NODE_EVICTION_OBSERVE=1 on the arm instead.
export BENCH_NODE_EVICTION=
# Agent prefix prefetch for `ours`, and only `ours`: 1 on, 0 off, empty leaves
# the arm alone. The workflow's own switch is spelled the other way round
# (KV_EVICTION_DISABLE_PREFETCH, which arms.toml pins to 0), and setting that
# in a file here does nothing because the arm always wins -- which is the
# whole reason for this variable.
#
#   BENCH_PREFETCH=0 ARMS=ours ./launch-batch.sh 50
#
# One variable is enough: the harness turns the disable flag into the two the
# prediction path reads, clearing LANGGRAPH_VLLM_AGENT_ENABLE *and*
# LANGGRAPH_VLLM_AGENT_BASE_URL, since either one alone re-enables it.
export BENCH_PREFETCH=
# The system prompt population phase, `ours` only: 1 runs it, 0 skips it,
# empty leaves the arm alone. It fills each node's prompt as far as the first
# runtime-only field ({messages}, {findings}, ...), POSTs the result to
# /v1/agents/prefetch so the server records the prefix under the node's agent
# id, and -- unless --no-prefill-on-miss -- prefills each one so it is
# resident in L1 before the first real request.
#
#   BENCH_SEED_PREFIXES=0 ARMS=ours ./launch-batch.sh 50
#
# Unlike the two above this is a command-line flag, spelled as a negative
# (--skip-system-prompt-population), so "on" is the absence of an argument.
# pitlane only adds it to a script that advertises it, which is why baseline
# and continuum cannot be reached by it: run_evaluate.py has no such phase.
export BENCH_SEED_PREFIXES=
# The prompt seeds, `ours` only: 1 on, 0 off, empty leaves the arm alone.
# compress_research and final_report_generation both open prompts that share
# no usable prefix with anything the server has computed -- 0 shared leading
# messages on 33/33 compress calls, 132 shared characters for the report -- so
# both prefill from scratch on the critical path. The seeds rebuild those
# prompts from requests that have already gone out and prefill them during a
# gap instead.
#
#   BENCH_PROMPT_SEEDS=0 ARMS=ours ./launch-batch.sh 50
#
# This is a speculative *prefill*, not a promotion: a wrong guess costs compute
# rather than a POST. It is affordable because the server admits such a phantom
# only into a step with no real request in flight and drops it if none arrives,
# so the cost lands on idle GPU time and HBM pressure. Worth its own arm.
export BENCH_PROMPT_SEEDS=

# --- workflow knobs (identical across arms; changing one invalidates the trace) ---
export LANGGRAPH_PROMPT_PSEUDO_DYNAMIC=0
export SUMMARIZATION_MODEL_MAX_TOKENS=8192
export RESEARCH_MODEL_MAX_TOKENS=10000
export COMPRESSION_MODEL_MAX_TOKENS=10000
export FINAL_REPORT_MODEL_MAX_TOKENS=10000
export TAVILY_CACHE_ENABLE=1
export TAVILY_CACHE_DIR=/disk2/vibhav/tavily_cache
# On a cache hit the query still goes out to Tavily and the live response is
# discarded in favour of the cached one, so `tavily_search.query` spans time a
# real search instead of a disk read. Trajectory-safe: the agent is handed the
# same frozen results either way, so the trace stays valid -- only wall-clock
# moves. Set it for every arm or none; one arm alone carries seconds of tool
# time the others don't. Costs a live API call per query, so rate limits apply.
export TAVILY_CACHE_PINNED=1
# A summarization that overruns falls back to the raw page, which is ~10x
# longer than its summary -- under pinned replay the next prompt then matches
# nothing in the trace and the question dies on a trace miss. It fires where
# the server is slowest, so it is biased toward whichever arm is losing: it
# cost continuum question 35 of the n50 batch. The first must stay BELOW the
# second, which is the outer limit on the same call.
export SUMMARIZATION_TIMEOUT_S=600
export ODR_HTTP_TIMEOUT_S=900
export ODR_FROZEN_DATE=2026-08-04

# --- tracing (optional) ---
export LANGSMITH_TRACING=true
export LANGSMITH_PROJECT=MTP
export LANGSMITH_ENDPOINT=https://api.smith.langchain.com

# --- the trace under test: same file for every eval arm ---
export ODR_TRACE_PATH=$TRACE_DIR/drb_v1_3.jsonl
